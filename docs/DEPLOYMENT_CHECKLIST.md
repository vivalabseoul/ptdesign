# 🚀 배포 전 체크리스트

## ✅ CSS 오류 수정 완료
- [x] `bg-primary/5` 오류 수정 완료

## 📋 배포 전 필수 확인 사항

### 1. 환경 변수 확인
현재 프로젝트에서 사용하는 환경 변수:
- `VITE_TOSS_CLIENT_KEY` (Toss Payments - 선택사항, 테스트 모드로 동작 가능)

**확인 방법:**
- `.env` 파일이 있는지 확인
- 배포 플랫폼(Vercel, Netlify 등)에서 환경 변수 설정 필요

### 2. Supabase 설정 확인
- [x] `users` 테이블에 `phone` 컬럼 추가 완료
- [ ] Supabase 프로젝트 ID와 API 키 확인
- [ ] RLS 정책 확인
- [ ] Edge Functions 배포 확인 (분석 기능)

### 3. 빌드 테스트
로컬에서 빌드가 성공하는지 확인:
```bash
npm run build
```

### 4. 배포 플랫폼 선택 및 설정

#### Vercel (추천) ⭐
1. GitHub에 코드 푸시
2. [Vercel](https://vercel.com)에서 프로젝트 Import
3. 빌드 설정 자동 감지됨
4. 환경 변수 설정 (필요시)

#### Netlify
1. GitHub에 코드 푸시
2. [Netlify](https://www.netlify.com)에서 프로젝트 Import
3. 빌드 명령: `npm run build`
4. 배포 디렉토리: `dist`

### 5. 배포 후 확인 사항
- [ ] 홈페이지 로딩 확인
- [ ] 회원가입/로그인 기능 확인
- [ ] 분석 기능 테스트
- [ ] 결제 기능 테스트 (Toss Payments)
- [ ] 관리자 페이지 접근 확인
- [ ] 모바일 반응형 확인

### 6. Supabase Edge Functions 배포
분석 기능을 사용하려면 Edge Functions도 배포해야 합니다:

```bash
# Supabase CLI 설치 (필요시)
npm install -g supabase

# 로그인
supabase login

# 프로젝트 링크
supabase link --project-ref YOUR_PROJECT_REF

# Functions 배포
supabase functions deploy server
```

### 7. 도메인 설정 (선택사항)
- 커스텀 도메인 연결
- SSL 인증서 자동 설정 (대부분의 플랫폼)

## 🔧 문제 해결

### 빌드 오류
```bash
# 의존성 재설치
rm -rf node_modules
npm install
npm run build
```

### 환경 변수 오류
- `.env` 파일은 Git에 커밋하지 않음 (`.gitignore` 확인)
- 배포 플랫폼에서 환경 변수 직접 설정

### CORS 오류
- Supabase Dashboard > Settings > API > CORS 설정 확인
- 배포된 도메인을 허용 목록에 추가

## 📝 배포 후 작업

1. **첫 관리자 계정 생성**
   - Supabase Dashboard > Authentication > Users
   - 또는 SQL Editor에서:
   ```sql
   UPDATE users SET role = 'admin' WHERE email = 'your-admin@email.com';
   ```

2. **Toss Payments 설정**
   - 프로덕션 키로 교체
   - 결제 성공/실패 URL 설정

3. **모니터링 설정**
   - 에러 로깅 (Sentry 등)
   - 분석 도구 (Google Analytics 등)

